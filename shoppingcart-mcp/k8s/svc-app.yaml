apiVersion: v1
kind: Service
metadata:
  name: mcp-app
  namespace: mcp
  labels:
    app: mcp-app
spec:
  type: ClusterIP
  selector:
    app: mcp-app
  ports:
  - name: http
    port: 8000
    targetPort: 8000
